---
import DentalArticle from '../../../../components/DentalArticle.astro';
import {treatments} from '../../../../data/dental';
export function getStaticPaths(){return treatments.map(treatment=>({params:{slug:treatment.slug},props:{treatment}}));}
const {treatment}=Astro.props;
---
<DentalArticle lang="tr" treatment={treatment}/>
