---
import ConstructionProject from '../../../../components/ConstructionProject.astro';
import { constructionProjects } from '../../../../data/construction';
export function getStaticPaths() {
  return constructionProjects.map(project => ({ params: { slug: project.slug.en }, props: { project } }));
}
const { project } = Astro.props;
---
<ConstructionProject lang="en" project={project} />
